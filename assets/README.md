# Assets Directory

This directory is a placeholder for game assets such as sprites and audio files.

Currently, the game uses:
- Canvas 2D API for rendering simple shapes (bird, pipes, ground)
- Web Audio API for generating simple beep sounds

If you want to add custom sprites or audio files, place them in this directory and update `src/main.js` to load them.
